// promise
// how to declarate a promise
const Promise = new Promesa((resolve, reject)) = {
    // codigo asincrono a resolver
    // ejemplo; llamdas apis, db,
    // leer archivos, etc
    resolve(cosaQueDevuelveSiSeEjecutaBien);
    reject(cosaQueDevuelveSiSeEjecutaMal);
}

// ejecucion de la promesa

Promise

    .then